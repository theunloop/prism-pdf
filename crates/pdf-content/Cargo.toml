[package]
name = "prismpdf-content"
description = "Content-stream operators & graphics state machine (ISO 32000 §8/§9)."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[lib]
# The package is published as `prismpdf-content`; the library keeps the name every
# dependent, test and bench already imports.
name = "pdf_content"

[lints]
workspace = true

[dependencies]
pdf-cos.workspace = true
