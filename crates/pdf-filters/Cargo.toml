[package]
name = "prismpdf-filters"
description = "Stream filters/codecs: Flate, LZW, ASCIIHex/85, RunLength, CCITTFax, DCT (ISO 32000 §7.4)."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[lib]
# The package is published as `prismpdf-filters`; the library keeps the name every
# dependent, test and bench already imports.
name = "pdf_filters"

[lints]
workspace = true

[dependencies]
pdf-cos.workspace = true
flate2.workspace = true
thiserror.workspace = true
zune-jpeg.workspace = true
hayro-jbig2.workspace = true
